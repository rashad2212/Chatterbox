script.js = (sr, remotecontrols, rr)
self(advert = disabled) for apps.new(chat)
insert:LiveChat
then > take
takeFrom > Models
local jsr.new(commandline) = bar2
invetnt.client = respetory.new(file)
local app = "install.js"

end
